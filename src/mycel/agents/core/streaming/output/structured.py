"""Output có cấu trúc: parse JSON còn dở dang để client hiển thị sớm từng field.

Field đã đóng thì phát ra ngay, không đợi dấu `}` cuối cùng.

Không đặt tên `json.py` để khỏi che module chuẩn cùng tên khi đọc code.
"""
