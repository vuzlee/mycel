"""Gom delta rời rạc thành output hoàn chỉnh, theo từng dạng output agent khai báo.

  text.py        nối delta thành văn bản
  structured.py  parse JSON dở dang, phát từng field khi nó đóng
  discard.py     không gom gì — chạy nền, chỉ cần kết quả cuối
"""
