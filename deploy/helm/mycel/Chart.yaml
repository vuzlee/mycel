apiVersion: v2
name: mycel
description: Multi-agent system tổng hợp báo cáo từ nhiều nguồn dữ liệu
type: application

# version = version của chart (sửa template thì tăng)
# appVersion = commit SHA của image (CI ghi đè lúc build)
version: 0.1.0
appVersion: "0.1.0"
