apiVersion: v2
name: mycel
description: Multi-agent system that builds reports from multiple data sources
type: application

# version = the chart's version (bump it when templates change)
# appVersion = the image's commit SHA (CI overwrites it at build time)
version: 0.1.0
appVersion: "0.1.0"
