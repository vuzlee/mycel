# Prometheus kéo metrics từ collector, không kéo thẳng từ app.
# App chỉ gửi OTLP tới một địa chỉ duy nhất; collector lo phần còn lại.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
