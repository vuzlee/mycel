# Prometheus scrapes metrics from the collector, not from the app directly.
# The app ships OTLP to one address; the collector handles the rest.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
