# Cấu hình môi trường dev. Bí mật để trong .env, không để ở đây.

[database]
schema_raw = raw
schema_silver = silver
schema_gold = gold

[pipeline]
batch_size = 500

[agents]
model = claude-sonnet-5
max_workers = 4
