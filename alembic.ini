# Alembic. The DSN is not here on purpose: `migrations/env.py` reads it from the same
# `Settings` the application uses, so a migration can never run against a different
# database than the code.
[alembic]
script_location = migrations
prepend_sys_path = src
path_separator = os
